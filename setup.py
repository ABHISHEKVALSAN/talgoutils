# write setup.py

from setuptools import setup, find_packages
import talgoutils


setup(
	name				=	'talgoutils',
	version				=	'1.0',
	packages			=	[],
	package_dir			=	{'talgoutils':	'talgoutils'},
	install_requires	=	[],
	zip_safe			=	False
)
